# 주식정보 자동매매 시스템 — CLAUDE.md

## 이 프로젝트란
월봉MA10 매매법 기반 미국/한국 주식 스캔 + 슬랙 알림 자동화. 실행 환경은
**로컬 PC가 아니라 GitHub Actions**(`.github/workflows/*.yml`)이 메인이다.
로컬 Windows 작업 스케줄러(`register_tasks.py`)는 별도의 중복 백업 경로일 뿐이다.

## 절대 하지 말 것 (NEVER DO)
- **월봉MA10 매매 규칙을 임의로 변경하지 말 것** (노후자산 시스템, 사용자 승인 없이 손절/익절 기준 수정 금지)
- 알림/스케줄 문제가 생기면 로컬 스케줄러부터 고치지 말 것 — `.github/workflows/`가 실제 실행 경로인지 먼저 확인
- GitHub Actions 잡이 "success"라고 해서 실제로 원하는 결과(슬랙 전송 등)가 나갔다고 가정하지 말 것 — 내부 예외가 조용히 삼켜질 수 있음
- 관리자 권한 없이 `register_tasks.py` 실행하지 말 것 — 기존 태스크 갱신이 조용히 실패함(Access denied가 나와도 스크립트가 계속 진행됨)

## 자주 하는 실수 (2026-08-01 세션에서 확인)
- PowerShell을 서브프로세스로 호출할 때 `encoding='utf-8'`을 쓰면 한글 시스템 메시지에서 깨짐 → **`encoding='cp949', errors='replace'`** 사용
- 멀티라인 PowerShell 스크립트를 `-Command`에 통째로 문자열로 넘기지 말 것 → **`.ps1` 파일로 저장 후 `-File`로 실행**
- `New-ScheduledTaskTrigger`엔 `-Monthly` 파라미터가 없음 → 월간 트리거는 **schtasks.exe**로 생성
- `New-ScheduledTaskSettingsSet`의 실제 파라미터명은 `-AllowStartIfOnBatteries`/`-DontStopIfGoingOnBatteries` (Disallow/Stop 아님, 반대 이름)
- `Set-ScheduledTask -InputObject`는 이 환경에서 "매개 변수가 틀립니다" 오류 발생 → `-TaskName` + `-Settings`/`-Principal` 객체 방식 사용
- Slack Block Kit은 메시지당 **50블록 하드 리밋** — `send_slack()`은 초과 시 자동 분할 전송하도록 이미 수정됨, 새 알림 블록 추가 시 유의
- 외부 스크래핑(`pd.read_html` 등)은 로컬에 우연히 깔린 패키지(lxml 등)에 의존하지 말고 `requirements.txt`에 명시할 것

## 문제 생기면 여기부터
1. `.github/workflows/`에서 관련 워크플로우 스케줄/최근 실행 확인 (`Invoke-RestMethod`로 GitHub API 조회 가능, 공개 저장소라 인증 불필요·로그 원문만 403)
2. `스케줄태스크_실행오류_수정_2026-08-01.md` — 이미 확인된 원인/조치 대조
3. 프로젝트 메모리(`project_stock_system.md`) — 계좌 구성, 슬랙 채널, 매매 전략 상세

## 핵심 규칙 요약 (2026-09-26 사용자 결정: 원서 원칙으로 전환)
- 매수 조건: **월말 종가로 확정된** 원서 신호만 — ① 돌파 = 10이평을 아래→위로 관통하는 양봉(후킹 캔들, p.256)
  ② 10이평 지지 반등 = 직전 달 10이평 위, 이번 달 저가가 10이평에 닿고 종가는 위(p.340). 코드: `book_patterns.buy_signal`
- 매도 조건: 월봉MA10 하향 이탈 (예외 없음)
- **주봉 눌림목 매수는 폐지**(월말 전 조기진입도 안 함). 근거: 88종목 50년 비교(`backtest_entry_timing.py`) —
  주봉 눌림 대기는 이득 없음, 조기진입은 29%가 월말 실패 손절·승률 하락, 우열도 통계적으로 불확실 → 사용자가 원칙 선택.
  `us_weekly_scan` 자동 실행(GitHub·로컬 태스크)도 중지. 매매법_전체_구현명세.md H4
- 우상향·매출성장 10%(사용자 규칙)·원서패턴·이익성장은 **제외 조건이 아니라 우선순위**(통합 스캔 1~3군, 패턴 스캔 ⭐)
- 이전의 "+5% 지지권/+15% 신규 매수 금지/+30% 고점권" 괴리율 구간은 원서에 없어 제거 — 괴리율은 표시만 한다
- 박스권(원서 p.308~309): 매수 신호 종목이 직전 12개월 고저 폭 ≤30%이고 그 고점을 못 넘었으면 "📦박스권 안" 표시 +
  후순위(제외 아님). `book_patterns.in_box`, 12개월·30%는 비교 검증으로 정한 구현값(`backtest_box_range.py`, 명세서 H1)
- DC/IRP 퇴직연금 계좌는 개별종목 트레이딩 규칙(즉시손절) 적용 대상 아님 — 장기 분산자산으로 별도 취급

## 월봉 vs 주봉 (2026-09-12 명문화 → 2026-09-26 주봉 매수 폐지)
성승현 원서의 매매법은 **순수 월봉 기준**이다(월말 종가 1회 확인, 매수·매도 전부 월봉MA10으로만 판단).
사용자가 "월말까지 기다리다 놓친다"는 이유로 주봉 눌림목 진입을 추가했었으나(사용자 실전 변형),
2026-09-26 진입 시점 비교 후 사용자가 **원서 원칙으로 돌아가기로 결정** — 주봉은 매수 판단에 쓰지 않는다.
- **월봉** = 유일한 신호. 매수·매도 전부 월말 종가 확정 월봉MA10 기준(위 "핵심 규칙 요약").
- **주봉·진행 중인 달** = 관찰 표시만("이번 달 돌파 진행 중 — 매수 아님"). 이것으로 매수·매도를 권하지 말 것.
- **차트 패턴 판정 기준은 `캔들차트(성승현작가)/패턴_구현명세.md` 하나로 고정(2026-09-26, 사용자 요청
  "모델 변경 때마다 바꾸는 것도 스트레스")**. 코드는 `book_patterns.py`, 잠금 테스트는 `tests/test_book_patterns.py`
  (조건값 잠금 + 원서 예시 차트 10개 재현, push 시 CI 자동). 패턴 해석을 바꾸려면 ① 원서 쪽수 근거 ② 원서 예시
  재현 결과 ③ 사용자 승인을 갖춰 명세서부터 고칠 것 — 원서 확인 없이 "원서와 다르다/버그다"라고 말하지 말 것
  (원서 PDF는 이미지라 Windows 한국어 OCR로 읽음, PDF 쪽 = 원서 쪽 − 250).
  핵심: 상승 패턴 완성 = 10이평을 아래→위로 관통하는 양봉(후킹, p.256), 하락 패턴 완성 = 10이평 이탈 음봉.
  패턴은 매수 조건이 아니라 매수 후보 **우선순위**·보유종목 **경고**용. 백테스트: 원서 패턴으로 시작한 상승의
  매매법 거래 평균 +42.8% vs 패턴 없는 후킹 +18.9%(명세서 §7).
- **패턴 외 원서 차트분석 규칙 전체(1~7장: 장대양봉 5%, 4등분선, 눌림 거래량 1/7, 정배열, 240이평, 돌반지,
  포킹, 탑다운 순서 등)는 `캔들차트(성승현작가)/매매법_전체_구현명세.md`에 쪽수·구현 상태와 함께 정리.**
  원서에 수치가 없는 곳(박스권 판정, 탑다운 강세/약세 비율 등)에 임의 숫자를 넣지 말 것 — 그 명세서의 H절(사용자 결정 대기)로 보낼 것.
- 패턴 분석 등 "장기 추세가 꺾였는가"를 묻는 판단은 전부 월봉 데이터로 할 것 —
  주봉/일봉으로 근사하면 호흡이 너무 짧아 오탐이 심각하게 늘어난다(2026-09-12 확인: 일봉 기준
  관심종목 46%가 오탐, 월봉으로 재보정 후 5% 미만으로 정상화).
