# 월봉매매법 규칙 변경 로그

이 파일은 `us_weekly_scan.py`(월봉+주봉 이중필터 매수조건)의 규칙이 바뀔
때마다 기록한다. 사용자 요청(2026-08-31): "항상 로그기록해줘" — 앞으로
규칙 변경은 반드시 여기 남기고 코드를 고친다.

---

## 2026-08-31 — 월봉 근접조건 추가 확정

**변경 전**: 월봉MA10 위이기만 하면 됨 (얼마나 위에 있는지 무관, 예: +59%도 통과)
**변경 후**: 월봉MA10 위 + 그 대비 **5% 이내**여야 후보 자격 (그 이상 뻗은 종목 제외)

**사용자 확정 근거**: "10이평선 한참위에 있는 종목은 이미 많이 올랐기 때문에
제외한다 / 월봉59는 탈락, 10이평선에 걸릴 때 종목이 선정된다"

**적용 위치**: `us_weekly_scan.py` `scan()` 함수, 월봉 필터 부분
```python
if not m_above or m_pct > ZONE_PCT:
    continue   # 월봉MA10 아래이거나 5% 초과로 뻗은 종목은 제외
```

**영향**: 오늘 스캔 기준 기존 5개 후보(SNDK+54%, MU+57%, NTAP+41%, ATI+30%,
NVDA+14%) 전부 이 조건으로 탈락. 신호 빈도가 크게 줄어듦 — 월봉도 근접해야
하므로 대기 기간이 길어질 수 있음.

**주의**: 이 변경 전, 같은 대화 안에서 사용자가 "월봉은 위에만 있고 주봉은
타이밍"이라고도 말씀하셔서 반대로 한 번 되돌렸다가 다시 이 조건으로
재확정함. 혼선이 있었던 지점.

---

## 매도 조건 관련 — 확인 필요 (아직 미반영)

사용자 언급: "매도종목도 월봉10이평선 아래로 내려오면 매도한다. 즉 -10%
떨어지면 무조건 매도"

CLAUDE.md 기존 규칙: "매도 조건: 월봉MA10 하향 이탈 (예외 없음)" — 특정 %
수치 없이 "이탈하면 무조건"이라고만 되어 있음.

**확인 완료 (2026-08-31)**: "-10%"는 새로운 수치 기준이 아니라 "이탈"을
설명하기 위한 예시 표현이었음. 사용자 확정: **"아래로 내려오면 무조건
알림이다. 매도는 내가 결정한다"** — 즉:
- 월봉MA10 하향 이탈 시 **알림은 예외 없이 무조건 발생** (기존 규칙 그대로,
  특정 %로 필터링하지 않음)
- 실제 매도 실행 여부는 사용자分이 알림 받은 뒤 직접 판단

→ 코드 변경 불필요. `slack_alert.py`의 `record_sell_signal`(월봉 이탈 시
무조건 기록) 로직이 이미 이 규칙과 일치함.

**추가 혼선 (같은 날 몇 분 뒤)**: "너는 이평선 이탈로 알려주고 -10%가
되면 매도알림"이라며 다시 특정 수치(-10%) 기준을 언급함 — 위 확인 내용과
상충. 사용자分이 "이런 규칙 있을 텐데 반드시 기록유지 / 당분간 변경하지
마라"고 정리하여, **매도 기준은 지금 이 시점부터 당분간 손대지 않기로
확정**함.

---

## 🔒 2026-08-31 기준 최종 확정 상태 (당분간 변경 금지)

**매수 조건** (`us_weekly_scan.py`): 월봉MA10 위 + 그 대비 5% 이내
**+** 주봉MA10 눌림목/지지권(등급 1~4는 주봉 기준, 단 월봉 5% 이내 통과한
종목만 대상)

**매도 조건** (기존 CLAUDE.md 규칙 유지, 미변경): 월봉MA10 하향 이탈 시
예외 없이 알림 발생 → 실제 매도는 사용자가 알림 받고 직접 판단. 특정
%(-10% 등) 수치 기준은 **도입하지 않음**.

**참고 — 아직 손 안 댄 것**: `tests/test_ma10_weekly_pullback.py`의
WFRESH/WPULL/WSUPPORT/WTREND/WBELOWW 테스트 케이스가 월봉을 매우 크게
확장된 값(105→200)으로 설정해둬서, 위 매수조건 변경 후 재실행하면 5건
실패로 뜬다. 이건 테스트가 새 규칙을 반영 못 해서 나는 실패이지 코드
버그가 아님 — "당분간 변경하지 마라"는 지시에 따라 지금은 손대지 않고
여기 기록만 남겨둠. 나중에 규칙이 최종 안정화되면 테스트 케이스를
새 규칙(월봉도 5% 이내)에 맞게 고쳐야 함.
