# 세션 요약 (2026-08-09)

긴 대화라 토큰 절약을 위해 여기까지 내용을 정리하고 새 세션에서 이어감.
(직전 요약: `세션요약_2026-08-02.md`)

## 결정된 것 (전부 커밋·push 완료)

### 2개월치 로컬 미커밋 작업물 정리
- `signal_tracker.py` — 이미 3개 스캔 스크립트(nasdaq100_scan.py/testa_scan.py/us_testa_scan.py)가
  import해서 쓰고 있었는데 커밋이 안 돼 있어 GitHub Actions에는 반영이 안 되던 상태 → 커밋 완료.
  이제부터 신규돌파 신호가 실제로 자동 기록됨.
- 함께 커밋: BNF 매매법 폴더(평균회귀 전략, 별도 트랙), discovery_scan/earnings_alert/
  portfolio_report·review/us_weekly_scan/compare_three_strategies/make_watchlist_excel.py
- 삭제: add_watchlist.py(register_watchlist.py와 중복), _gen_signals.py·_quick_scan.py·
  find_pos.py(일회성 스크래치)
- `.gitignore`에 추가: 스캔 결과 데이터 파일(signal_log.json 등)과 개인 문서/자료
  (docx, bat, 옵시디언 폴더 등) — git status 노이즈 제거 목적, 파일 자체는 로컬에 그대로 있음
- 커밋: `c77097f`, `3ce338b`

### 월봉MA10 매매법 로직 검증 테스트 추가
- 기존 verify_system.py/test_alerts.py는 "시스템이 안 죽고 도는지"(웹훅, 티커, 신호파일 최신성)만
  검증했지 "신호 계산 로직 자체가 규칙대로 정확한가"는 검증하는 게 없었음
- `tests/test_ma10_monthly_signal.py`, `tests/test_ma10_weekly_pullback.py` 추가 — 합성 가격
  데이터로 yf.download/fetch를 대체해 실제 프로덕션 함수(scan_ndx100, scan_sp500,
  us_weekly_scan.scan)를 네트워크 호출 없이 검증. 38개 체크 전부 통과 — 신규돌파/지지권/
  추세권/고점권 분류, MA10 이탈시 제외, 주봉 눌림목(≤5%) 판정, 적정매수단가·손절가 계산까지
  전부 규칙대로 정확히 동작함을 확인. 매매법 규칙 자체는 변경하지 않음.
- `tests.yml`에 두 단계 추가 — main push 시 자동 실행
- 커밋: `3616950`

### 매매 규칙 확정 (사용자 정리 버전)
- **DC계좌**: 종목당 70% 매수 → -10% 하락시 30% 추매(평단 낮추기) → 장기 보유. 단, 펀더멘털이
  훼손된 경우(예: SMR — 데이터센터 인허가 지연·지역주민 반대) 하락해도 추매 대상에서 제외.
  DC/IRP는 개별종목 즉시손절 규칙 적용 대상 아님(CLAUDE.md 기존 규칙과 일치).
- **개별계좌(미래에셋 등)**: 고확신 종목은 100% 수익 도달시 50% 매도(원금 정확히 회수) → 나머지
  홀딩. 확신 낮은 종목은 30% 수익에 50% 또는 전량 매도. -10% 하락시 30% 추매.
- 100%가 이론적 EV 최적은 아님(백테스트상 150~200%가 더 나음)을 확인했지만, 원금회수라는
  심리적 명확성 + 리스크가 임계값에 따라 크게 안 달라진다는 점에서 실용적 타협점으로 채택.

## 논의했으나 결론만 내고 세부 미확정인 것
- 물타기(-10%→30%추매) 트리거를 종목별 과거 변동성에 맞춰 차등화하는 것 — 제안만 하고 구현 안 함
- 계좌 내 종목 비중 상한선(편중 방지, CRWD 문제의 근본 해법) — 제안만 하고 수치·구현 미정
- CRWD: 88%→100% 도달 시 50% 매도 대기 중, 호르무즈 협상 기대감으로 미국증시 변동성 커서 관망 중
- MRVL: 관심종목. 월봉MA10 위(+45%), 주봉MA10 대비 -8.5%로 눌림목 근접 — 매수 조건 거의 충족

## 테마ETF 익절 임계값 시뮬레이션 (참고용, 규칙 변경 아님)
DC계좌 테마ETF 9종목 전체 가격이력(2020~2026)으로 20~200% 임계값별 절반익절 백테스트.
결론: 단일 숫자가 아니라 종목 성격별로 다름 — 2차전지(364980)·SMR(0051G0)류는 붐-버스트가 커서
조기 익절(30~50%)이 유리, 미국테크TOP10(381170)·차이나AI테크(428510)류는 상대적으로 꾸준히
올라서 100%까지도 홀딩이 유리. 도달후 낙폭 중앙값은 임계값 무관하게 -12~14% 수준.

## BNF 매매법 관련 확인
"하락장에 유리하다"는 "하락장에도 잡힌다"가 아니라 "패닉 투매 후 반등 확인 시점을 잡는다"는 뜻.
지금(고점대비 30~40% 그라인딩 하락) 신호가 안 잡히는 이유를 실제 스캔으로 진단: RSI 과매도
조건이 0% 충족(59종목 중 0개) — 완만한 하락은 RSI가 중립권에 머물러 과매도가 잘 안 나옴.
MACD 0선 상향돌파(반등 확인)도 아직 미충족 — 설계대로 정상 작동 중, 버그 아님.

## 인프라/기타
- NAS(시놀로지) 여분 제안 받음 — 자동화 시스템 개선 목적으로는 부적합(GitHub Actions를 쓰는
  이유 자체가 "집 장비 상시 가동 불필요"였는데 정반대 방향) 결론, 필요 없으면 반납/양도하기로
- TQQQ 3배 레버리지 자동매수법(후배 사례) — 리스크 성격이 완전히 다르고("검증 안 된 자동실행
  위험" 기존 원칙과 배치) 생존편향 가능성 큼, 신중 접근 권고만 하고 실행 안 함
- 파인튜닝 개념 논의 — Claude는 개인 파인튜닝 옵션 자체가 없음, 지금 방식(메모리+CLAUDE.md+git)이
  이런 용도엔 정석적 접근이라는 결론
- PC 보안: nProtect 팝업(TrustPKI Enterprise 취약점, 원격코드실행+임의파일다운로드) — 게시자
  확인 + KISA 공식 공지로 진위 검증 후 "취약점 해결하기"로 정상 조치 완료

## 다음 할 일
- CRWD 100% 도달 여부 계속 확인, 도달 시 50% 매도 실행
- MRVL 주봉MA10 눌림목 완성 여부 확인 후 매수 검토
- (원하면) 계좌 비중 상한선 규칙 구체화, 물타기 트리거 변동성별 차등화 구현
- 8월 말 월말 알림 정상 수신 확인

## 참고 문서
- `세션요약_2026-08-02.md` — 직전 요약(하네스·테스트 인프라, 미래에셋/DC 계좌 매매 결정)
- `CLAUDE.md` — 프로젝트 규칙 및 자주 하는 실수
- `project_stock_system.md` (메모리) — 계좌 구성, 슬랙 채널, 전략 상세
- `feedback.md` (메모리) — 거시경제/뉴스 종합 답변 시 인과관계 단정 주의 항목 추가됨
