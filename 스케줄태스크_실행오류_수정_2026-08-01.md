# 스케줄 태스크 실행 오류 수정 (2026-08-01)

## 증상

7월 말(7/28~7/31) 월봉MA10 종목 알림이 슬랙으로 오지 않음.

## 원인 (2가지가 겹침)

### 1. 태스크 로그온 방식이 "로그온 중일 때만 실행"

Windows 작업 스케줄러 태스크가 기본값인 `Interactive only` 로그온 방식으로 등록되어 있었음.
PC 화면이 잠겨있으면 예정 시각에 실행되지 않고 지연되다가, 나중에 화면이 열렸을 때
뒤늦게 실행을 시도하며 `0x800710E0` ("관리자가 요청을 거부함") 에러로 실패함.

- `Stock_Monthly_28`: 16:10 예정 → 20:11에 실행 시도, 실패
- `Stock_Monthly_29`: 16:10 예정 → 19:21에 실행 시도, 실패
- `Stock_Monthly_31`: 7/31 트리거 자체가 발동하지 않음 (직전 성공 기록이 5/31에 멈춰있음)

### 2. `register_tasks.py`가 관리자 권한 없이 실행되면 갱신 실패를 조용히 무시함

기존 스크립트는 `schtasks /create /f`로 태스크를 갱신했는데, 관리자 권한이 아닌 터미널에서
실행하면 이미 존재하는 태스크에 대해 `Access is denied` 에러가 나면서 **갱신이 안 됨**.
그런데 스크립트가 이 에러를 그냥 콘솔에 출력만 하고 계속 진행해서, 나머지 태스크가
"OK"로 잘 나오면 전체가 성공한 것처럼 착각하게 됨.

→ 이번 세션 초반 `US_Weekly_Scan` 태스크를 추가하며 `register_tasks.py`를 관리자 권한 없이
재실행했었는데, 이때 `Stock_Weekly_Friday`와 월말 4종 태스크(`Stock_Monthly_28~31` 등)가
전부 갱신에 실패한 채 예전 버전(`slack_alert.py auto`, 예전 로그온 방식)으로 남아있었음.
"완료됐다"고 여기고 넘어갔던 게 재발의 직접 원인.

## 조치

`register_tasks.py` 전면 재작성:

1. **관리자 권한 필수 체크** — 관리자 권한이 아니면 아무 작업도 하지 않고 즉시 중단
   (실패를 조용히 넘어가는 걸 원천 차단)
2. **모든 태스크를 `LogonType=S4U`로 전환** — 비밀번호 저장 없이도 로그오프/화면잠금
   상태에서 정상 실행됨 (기존 "로그온 중일 때만" 방식 폐기)
3. **등록 직후 자동 검증** — 각 태스크를 재조회해서 `Found / LogonType / Enabled / NextRun`을
   표로 출력. "OK"라는 문구만 믿지 않고, 실제로 의도대로 등록됐는지 매번 확인.
4. **모든 출력을 `register_tasks_last_run.log`에 실시간 기록** — 관리자 권한 창은
   UAC 승인 후 별도 콘솔에서 뜨기 때문에, 원격/사후에도 무슨 일이 있었는지 항상
   파일로 확인 가능하게 함.

## 검증 결과 (2026-08-01 15:25 기준)

등록된 25개 태스크 전부 `Found=YES, LogonType=S4U, Enabled=True`, 다음 실행시각 정상 확인.

## 재발 방지 — 앞으로 지킬 것

- 스케줄(`register_tasks.py`)을 바꿀 일이 생기면 **반드시 관리자 권한 PowerShell/터미널**에서
  실행할 것. 관리자 권한이 아니면 스크립트가 즉시 멈추도록 만들어 놨으니, 멈추면 그게
  신호임.
- 실행 후 `register_tasks_last_run.log`의 검증 표에서 대상 태스크가 `S4U`인지 반드시 확인.
- "OK: 태스크명"이 콘솔에 찍혔다고 해서 실제로 최신 상태로 갱신됐다고 가정하지 말 것 —
  이번 사고가 정확히 이 가정 때문에 발생함.

---

## 정정 (같은 날 추가 조사) — 진짜 원인은 로컬이 아니라 GitHub Actions였음

위 내용은 로컬 PC의 Windows 작업 스케줄러 얘기인데, **실제 알림은 GitHub Actions
(`.github/workflows/*.yml`)에서 클라우드로 실행되고 있었다.** 로컬 스케줄러는 완전히
별개의 중복(백업) 경로였고, 이번 "알림 미수신"의 진짜 원인이 아니었음. 로컬 쪽을 먼저
고친 건 헛다리였고, GitHub Actions 실행 이력(API)을 직접 조회해서 진짜 원인 두 가지를
찾음.

### 원인 A: S&P 500 알림 — Wikipedia 스크래핑이 GitHub Actions에서만 실패

`sp500_scan.py`의 `get_sp500_tickers()`가 Wikipedia에서 종목 리스트를 못 가져오면
`sys.exit(1)`로 조용히 죽는 구조였음. 실제로 6월부터 매번(`sp500_scan.yml`) 실패
(`conclusion: failure`)하고 있었는데, 로컬에서는 `lxml`/`bs4`/`html5lib`가 우연히
이미 깔려있어서(다른 패키지의 부수 설치물) 문제를 못 느꼈던 것으로 보임.
`requirements.txt`에는 이 파서들이 명시돼 있지 않았음.

**조치**: `requirements.txt`에 `lxml` 명시 추가, `get_sp500_tickers()`에 재시도(최대 3회,
지수 백오프) + 상세 예외 로그(`traceback.print_exc()`) 추가.

### 원인 B: 김학주교수(메인) 채널 알림 — Slack 블록 50개 한도 초과

Slack Block Kit은 메시지 하나당 블록 50개가 하드 리밋인데, `build_monthly_alert()`가
54개 종목 전체 스캔 결과 + 포트폴리오 + 체크리스트를 한 메시지에 다 넣다 보니
**실제로 53개 블록**이 생성됨을 로컬 재현으로 확인. 50개 초과 요청은 Slack API가
400으로 거부하는데, `send_slack()`의 `except urllib.error.URLError`가 이를 조용히
잡아서 `False`만 반환하고 스크립트는 정상 종료(exit 0)됨 — 그래서 같은 실행에서
연금계좌 채널(블록 수가 훨씬 적음)은 갔는데 메인 채널만 안 간 것.

**조치**: `send_slack()`에 50블록 초과 시 자동으로 여러 메시지로 분할 전송하는 로직
추가(`_send_slack_raw` + 청크 분할). 정보 손실 없이 항상 전달되도록 함.

### 교훈

- **"성공(success)"으로 끝난 작업이 실제로 원하는 일을 다 했다는 보장은 없다.**
  GitHub Actions 잡 자체는 매번 exit 0로 "성공" 표시됐지만, 내부적으로
  `send_slack()`이 `False`를 반환하며 조용히 실패한 케이스였음 — HTTP 에러를
  잡아서 로그만 찍고 넘어가는 방어적 코드가, 실패를 눈치채기 어렵게 만드는
  역설을 만듦.
- 로컬 스케줄러부터 고친 건 실제 배포 구조(GitHub Actions 사용 여부)를 먼저
  확인하지 않고 진단한 실수였음. 다음부터는 `.github/workflows/`부터 확인할 것.
