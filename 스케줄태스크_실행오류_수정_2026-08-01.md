# 슬랙 알림 미수신 문제 조사 및 수정 (2026-08-01)

## 증상

7월 말(7/28~7/31) 월봉MA10 종목 알림이 슬랙으로 오지 않음.
구체적으로: 나스닥100·연금계좌·테스타스캔은 정상 수신, **김학주교수(메인) 채널과
S&P500 채널만 누락**.

## 시스템 구조 확인

이 프로젝트의 알림은 로컬 PC가 아니라 **GitHub Actions**(`.github/workflows/*.yml`)에서
클라우드로 실행된다. 로컬 Windows 작업 스케줄러(`register_tasks.py`)는 별도의
중복(백업) 경로이며, 이번 문제의 원인이 아니었다. (참고: 로컬 스케줄러 자체는
"화면 잠금 시 실행 실패" 문제가 있어 별도로 S4U 로그온 방식으로 개선해 두었다.)

GitHub Actions 실행 이력(API)을 직접 조회해서 실제 원인 두 가지를 확인했다.

## 원인 1 — S&P500 알림: Wikipedia 스크래핑이 클라우드에서만 실패

`sp500_scan.py`의 `get_sp500_tickers()`가 Wikipedia에서 종목 리스트를 못 가져오면
`sys.exit(1)`로 조용히 실패하는 구조. 실행 이력을 보니 **6월부터 매번 실패**
(`conclusion: failure`)하고 있었다.

- Wikipedia 페이지 파싱(`pd.read_html`)에는 `lxml`/`html5lib`/`beautifulsoup4` 중
  하나가 필요한데, `requirements.txt`에 명시돼 있지 않았다.
- 로컬 PC에는 이 파서들이 다른 패키지의 부수 설치물로 이미 깔려 있어서 문제를
  못 느꼈지만, GitHub Actions는 매번 `requirements.txt` 기준 클린 설치라 실패했다.

**조치**
- `requirements.txt`에 `lxml` 명시 추가
- `get_sp500_tickers()`에 재시도(최대 3회, 지수 백오프) 추가
- 실패 시 `traceback.print_exc()`로 상세 예외 로그 남기도록 개선

## 원인 2 — 김학주교수 채널: Slack 블록 50개 한도 초과

Slack Block Kit은 메시지 하나당 블록 50개가 하드 리밋이다. `build_monthly_alert()`가
54개 종목 전체 스캔 결과 + 포트폴리오 + 실행 체크리스트를 한 메시지에 다 담다 보니,
로컬 재현 결과 **실제로 53개 블록**이 생성됨을 확인했다(한도 초과).

- 블록 50개 초과 요청은 Slack API가 HTTP 400으로 거부한다.
- `send_slack()`의 `except urllib.error.URLError`가 이 에러를 조용히 잡아 `False`만
  반환하고 스크립트는 정상 종료(exit 0)됐다. 그래서 같은 실행에서 블록 수가 적은
  연금계좌 채널은 정상 전송됐는데, 메인 채널만 소리 없이 실패했다.

**조치**
- `send_slack()`에 50블록 초과 시 자동으로 여러 메시지로 나눠 보내는 로직 추가
  (`_send_slack_raw` + 청크 분할). 종목이 아무리 많아도 정보 손실 없이 전달됨.

## 검증

- `sp500_scan.py`, `slack_alert.py` 로컬 import/문법 검증 완료
- `build_monthly_alert()` 실제 실행해 블록 수 재현(53개 → 원인 확정)
- 수정사항 커밋 후 GitHub 원격(`main`)에 push 완료 — 다음 스케줄부터 반영됨

## 교훈 / 재발 방지

1. **"성공(success)"으로 끝난 작업이 실제로 원하는 일을 다 했다는 보장은 없다.**
   GitHub Actions 잡 자체는 exit 0로 "성공" 표시됐지만, 내부적으로 `send_slack()`이
   `False`를 반환하며 조용히 실패했다. 에러를 잡아서 로그만 찍고 넘어가는 방어적
   코드가, 오히려 실패를 눈치채기 어렵게 만들 수 있다.
2. 문제를 진단할 때는 **실제 배포 구조부터 확인**할 것 — 이번에도 로컬 스케줄러부터
   고치다가 뒤늦게 `.github/workflows/`가 진짜 실행 경로라는 걸 발견했다. 다음부터는
   `.github/workflows/` 존재 여부를 가장 먼저 확인한다.
3. 종목 수·데이터량이 늘어나는 시스템에서 외부 API(Slack 등)의 하드 리밋을 우회하는
   방어 로직(분할 전송 등)은 처음부터 넣어두는 게 안전하다.
4. 외부 스크래핑(Wikipedia 등)에 의존하는 코드는 로컬 개발 환경에 우연히 깔린
   패키지에 의존하지 말고, 실제 필요한 의존성을 `requirements.txt`에 전부 명시할 것.
